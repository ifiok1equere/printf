#include "main.h"

/**/

int print_rot(va_list list)
{
	char *str = va_arg(list, char *);
	int i_str, i_alpha = 0;
	char *alpha = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz";
	/*char *rot = "NOPQRSTUVWYZABCDEFGHIJKLMnopqrstuvwxyzabcdefghijklm";*/

	for (i_str = 0; str[i_str]; i_str++)
	{
		if (str[i_str] == alpha[i_alpha])
		{
			/*output character with same index as i_alpha from rot_buf*/
		}
		if (str[i_str] != alpha[i_alpha])
		{
			/*output the same character i.e str[i_str]*/
		}
	}

	
}
